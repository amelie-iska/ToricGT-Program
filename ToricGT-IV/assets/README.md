# ToricGT IV source bundle

Compile with:

```bash
latexmk -pdf -interaction=nonstopmode -halt-on-error toricgt_iv_oracle_trajectory_classifiers.tex
```

The source is self-contained and uses TikZ for the diagrams.
